# 2026-05-08 수정 내역

## 1. 샘플 데이터 추가 — 4/27 ~ 5/3 주간
- `handleSeedData`에 4월 27일~5월 3일 주간 인증 데이터 추가
  - 김도연 (certMethod=1, 목표 3회): 4/27, 4/29, 5/1
  - 오은혜 (certMethod=2, 목표 2회): 4/28 ×2장, 4/30 ×2장
  - 이병권 (certMethod=1, 목표 3회): 4/28, 4/30, 5/2

## 2. 멤버 선택 셀렉트 위치 변경
- 기존: 달력 섹션 상단 별도 영역
- 변경: `cal-section-top` 안 왼쪽 배치, 주/월·오늘 버튼은 오른쪽 배치

## 3. 주 보기 인증 원 사이즈 확대
- `.day-cert-name-tag` 크기: 26px → 30px

## 4. 반쪽 원(D자형) 표시
- certMethod=2인 멤버가 하루 중 1장만 업로드한 경우 반쪽 원(D자형)으로 표시
- 기존 방식(회색 원에 컬러 50% 오버레이) → 실제 D자형 반쪽 원으로 변경
- 주 보기·월 보기 모두 동일 적용

## 5. 전체 설정 — 타이틀 수정 기능 추가
- 전체 설정 팝업에 타이틀 입력 필드 추가 (최대 20자)
- Firestore `settings/global`에 `title` 필드 저장
- 헤더 타이틀이 실시간으로 반영됨
- 기본값: "운동 인증"

## 6. 카메라 타임스탬프 위치 변경
- 기존: 화면 가운데
- 변경: 왼쪽 하단
- 촬영된 사진 캔버스 합성 좌표도 동일하게 적용

## 7. 카메라 사진 번호(1/2) 위치 변경
- 기존: 하단 가운데
- 변경: 오른쪽 하단
- CSS 오버레이 및 캔버스 합성 모두 적용

## 8. 카메라 사진 번호 표시 조건 개선
- certMethod=2인 사람이 카메라를 열면 대기·촬영·미리보기 전 상태에서 1/2, 2/2 표시
- photoIndex 계산 방식 변경: `todayCount + 1` → `(todayCount % certMethod) + 1` (순환 계산)

## 9. 썸네일에 사진 번호 뱃지 추가
- certMethod=2 멤버의 인증 썸네일 오른쪽 하단에 `1 / 2`, `2 / 2` 뱃지 표시
- 같은 날 같은 유저의 사진 순서를 기준으로 자동 계산
